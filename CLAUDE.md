# CLAUDE.md — Table Talkers (Web)

이 파일은 Claude Code가 매 세션 읽는 프로젝트 규칙이다. 짧고 강제적으로 유지한다.
제품/설계 상세는 `docs/table-talkers-design-doc.md`, 웹 실행 계획은 `docs/BUILD_PLAN_WEB.md` 참조.

> **2026-07-04 피벗:** 접근성 때문에 스팀 → **웹 기반**으로 전환. `Assets/`, `Packages/`, `ProjectSettings/`의
> Unity 트리는 **동결된 프로토타입**(참고용)이며 활성 개발은 전부 **`web/`** 에서 한다.
> 스팀 버전은 훗날 웹을 감싸는 래퍼로 재등장할 수 있다.

---

## 프로젝트

3D 공간에서 원탁에 둘러앉아 실시간으로 **대화**하는 소셜 시뮬레이터. **브라우저 / 링크 입장.**
게임 메커닉 없음. 핵심 가치 = **존재감 있는 실시간 대화.**

## 핵심 원칙 (코드 결정을 제약하는 것만)

1. **대화가 제품이다.** 스코어·승패·미니게임을 추가하지 않는다.
2. **존재감 > 그래픽 충실도.** "누가 누구를 보는지 / 말하는지 / 웃었는지"가 최우선.
3. **지연이 최우선 지표.** mouth-to-ear ≤150–200ms. 디버그 오버레이에 상시 표시.
4. **입장은 링크 하나.** 게스트는 URL 클릭 + 마이크 허용만으로 방에 들어온다. 계정·설치·결제 없이.
5. **상시 서버 최소.** 시그널링(접속 중개)만 경량 서버, 미디어/상태는 P2P(WebRTC). 방 트래픽을 서버로 라우팅하는 구조(SFU/TURN 상시 의존)는 규모가 요구할 때만.
6. **초대제 우선.** 방 코드/링크를 아는 사람만. 공개방/발견 기능은 지금 만들지 않는다.

## 기술 스택 (확정 + 기본값)

- 클라이언트: **Vite + TypeScript + Three.js** (경량 3D, 코지 라운지)
- 네트워킹: **WebRTC 메시** — 음성(오디오 트랙) + 상태(데이터 채널). 방 생성자 = 호스트(권위).
- 시그널링: **Node 경량 WebSocket 서버**(`web/server/`) — 로컬 개발 기본, 추후 무료 티어 클라우드 배포.
- 음성 품질: 브라우저 내장 **AEC/NS/AGC** (`getUserMedia` 제약으로 활성화). 코덱 Opus.
- STUN: 공용 STUN 사용. **TURN 없음(MVP)** — 일부 엄격한 NAT에서 연결 실패 가능(문서화된 한계).
- 결제(후순위): 웹 MoR(Paddle류) — 호스트 페이 → 방 전체 격상. 게스트는 항상 무료.

## 아키텍처 불변식 (MUST — Unity 시절과 동일한 정신)

- **참가자 추상화.** 좌석 주체는 `Participant` 인터페이스로만. 사람 가정을 박지 말 것.
- **음성 추상화.** 프레즌스/UI 코드는 `VoiceService` 인터페이스에만 의존. WebRTC 구현은 그 뒤에.
- **호스트 권위.** 좌석 배정 등 방의 진실 소스는 호스트 피어. 게스트는 보고하고 보간한다.
- **동기화 상태 최소.** 좌석 맵 · 헤드 yaw/pitch(10–20Hz) · 발화 여부(수신 오디오에서 파생 우선) ·
  이모트 이벤트(일회성) · 이름. 그 외를 네트워크로 보내지 않는다.
- **방 = 컬렉션.** "아바타+테이블" 하드코딩 금지. 미래 공유 표면(화면/문서) 자리를 남긴다.

### 절대 하지 말 것 (지금 범위 밖)
녹화·전사 / 공개방·발견 / 구독·Enterprise / 화면공유 구현 / 계정 시스템(호스트 결제 전까지) / 호스트 마이그레이션(주석으로 한계만 명시).

## 코드 구조 & 컨벤션 (web/)

```
web/
  index.html            # 진입 + 최소 CSS/HUD 마크업
  src/
    core/        # Participant 인터페이스, 레지스트리, 설정 상수
    net/         # 시그널링 클라이언트, RoomNetwork(WebRTC 메시, 좌석 권위)
    voice/       # VoiceService 인터페이스 + WebRtcVoice(레벨/뮤트/볼륨/팬)
    scene/       # Three.js 라운지, 아바타(생명감/이모트)
    player/      # 착석 1인칭 카메라(yaw/pitch 클램프)
    ui/          # HUD(방 패널/디버그/힌트), 로컬라이즈 키 스텁
  server/signal.js       # 경량 시그널링 (Node + ws)
```

- TypeScript strict. 이름: PascalCase(타입), camelCase(변수/함수). 매직넘버는 `core/config.ts`로.
- 주석/식별자 영어. UI 문자열은 로컬라이즈 키 스텁 경유.
- 에이전트가 `npm run dev`(vite) + `npm run signal`(시그널링)로 직접 실행·검증한다.

## 작업 분담

**Claude Code:** 코드 전부, 로컬 실행·빌드·미리보기 검증, 씬/아바타 구성(코드로), 시그널링 서버.
**사람:** 실제 마이크로 2탭/2기기 대화 체감, 도메인·배포 계정(원할 때), 결제 계정, 아트 에셋 선정.

## 완료 기준(DoD) & 마일스톤

- 태스크 완료 = `tsc --noEmit` + `vite build` 통과 + 불변식 위반 없음 + 해당 태스크 완료 기준.
- 태스크마다 커밋(W번호 명시).
- **M0(web):** 한 컴퓨터에서 브라우저 탭 2개로 — 서로의 헤드 방향이 보이고 음성 대화가 된다.
- **M1(web):** 링크만 받은 외부 1인이 접속해 4인까지 자연스러운 대화(립싱크·발화표시·이모트).
